// Copy this file to `config.js` and fill in your Supabase project values.
// `config.js` is in .gitignore? Up to you — these are PUBLIC anon keys, safe to
// commit when Row Level Security (RLS) is enabled on every table.
//
// Where to find these:
//   Supabase dashboard → Project Settings → API
//     - Project URL                  → SUPABASE_URL
//     - Project API keys → anon/public → SUPABASE_KEY
//
// NEVER paste the `service_role` key here. That key bypasses RLS.

window.SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
window.SUPABASE_KEY = 'YOUR-ANON-PUBLIC-KEY';
